<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.courierdelivery">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/courier"
        android:label="@string/app_name"
        android:roundIcon="@drawable/courier"
        android:supportsRtl="true"
        android:theme="@style/Theme.CourierDelivery">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC556YLU4jEWJCcha3IMb4vwKS3v-x22tw" />

        <activity
            android:name=".TrackPerson"
            android:label="@string/title_activity_track_person"></activity>
        <activity android:name=".ShowOrders" />

        <activity android:name=".ShowCouriers" />
        <activity android:name=".ShowRequests" />
        <activity android:name=".SendRequest" />
        <activity android:name=".UserDashboard" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".UserRegister" />
        <activity android:name=".UserLogin" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
    </application>

</manifest>